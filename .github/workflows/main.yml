name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install deps
      run: npm install
    - name: Build
      run: make
    - name: Test
      run: npx elm-test
